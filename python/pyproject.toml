[project]
name = "r-gather"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "numpy",
    "networkx",
    "matplotlib",
]

[project.optional-dependencies]
dev = ["pytest"]

[tool.pytest.ini_options]
pythonpath = ["."]
addopts = ["--ignore=tests/test_visualization.py"]
